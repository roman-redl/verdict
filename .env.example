# Scite API (optional: free tier works without a key; key: scite.ai/users/me/api)
SCITE_API_KEY=

# Consensus API (reserved for the mode-B discovery integration — a roadmap
# item; the Go code does NOT read this yet. MCP auth is configured
# separately via `claude mcp add`, not here. Key: consensus.app → settings)
# CONSENSUS_API_KEY=

# Semantic Scholar (optional key: raises the free anonymous rate limits — the
# pipeline makes one batched request per run anyway)
S2_API_KEY=

# PubPeer developer key (optional, issued on request at pubpeer.com) — enables
# the post-publication commentary check
PUBPEER_DEVKEY=

# OpenAlex asks for an e-mail to join the "polite pool" (optional but nice)
OPENALEX_EMAIL=

# ── LLM backend for analytical stages (critique + synthesis) ──
# "" (auto) | anthropic | claude-cli | openai-compat
LLM_BACKEND=

# claude-cli: a Claude Code wrapper from ~/.zshrc — reuses existing
# subscriptions, no separate tokens to buy.
# WARNING: claude-zai is one-way — the first request blocks the corporate
# gateway for hours of cooldown. Corporate wrappers have no such effect.
LLM_COMMAND=claude-zai

# Exact model name for the wrapper (aliases silently fall back to default).
# Empty = wrapper default (claude-zai → glm-5.3, 1M context).
LLM_MODEL=

# Per-stage model overrides (exact names; empty = LLM_MODEL): a cheap model
# for the per-paper critique, a strong one for the synthesis/judge call.
LLM_MODEL_CRITIQUE=
LLM_MODEL_SYNTHESIS=

# Per-call LLM timeout, seconds (synthesis over 15 papers can take a while)
LLM_TIMEOUT=600

# ── Anthropic API (direct access; for the future, when personal keys appear) ──
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5

# ── Any OpenAI-compatible endpoint: OpenRouter / DeepSeek / vLLM / Together ...
# The migration path to cheap personal subscriptions.
OPENAI_BASE_URL=
OPENAI_API_KEY=
# (the model name for this backend goes in LLM_MODEL)

# How many papers to take from discovery
MAX_PAPERS=15
# Enrichment parallelism (OpenAlex etc.)
CONCURRENCY=4

# Per-topic DOI enrichment cache (OpenAlex/Crossref lookups), days; 0 disables.
# Challenge re-runs of the same topic reuse it instead of re-querying.
ENRICH_CACHE_TTL_DAYS=30

